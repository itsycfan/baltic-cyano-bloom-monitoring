# Proposal (SOP-1 output)

> This is the one artifact SOP-2 needs. Once this is committed and tagged `v0-proposal`, `docs/00_project_definition.md` and `01_literature_review.md` are reference material, not active documents.

## Research Questions

*One row per RQ. Keep the "concluded when" column concrete and checkable — it is what `RQ_MAPPING.md` will track against.*

| ID | Question | Concluded when… |
|---|---|---|
| RQ1 | | |
| RQ2 | | |

## Research Roadmap (v0)

*The RQs above, laid out as a sequence or dependency graph — which get tackled first, and why. Once an image exists, place it at `reports/images/research_roadmap.png` and reference it here.*

## Technical Framework (v0)

*The system/pipeline shape implied by the RQs — inputs, stages, outputs. This is a hypothesis at this stage, not a build plan. Once an image exists, place it at `reports/images/technical_framework.png` and reference it here.*

## Out of scope

*Explicitly excluded from this round, carried from `00_project_definition.md` §4.3, plus anything the literature review ruled out.*

---

When this is stable: commit, tag `v0-proposal`, link this document from the project's Obsidian entry note, and start SOP-2 by initializing `RQ_MAPPING.md` with the table above.
