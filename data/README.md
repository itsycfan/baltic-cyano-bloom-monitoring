# data/

Raw data lives here, kept local only — everything except this file is git-ignored (see `.gitignore`). Put a small representative subset in `../demo_data/` instead so the repository stays clonable and runnable without the full dataset.
