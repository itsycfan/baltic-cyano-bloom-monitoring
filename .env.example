# Copy to .env.local and fill in — .env.local is git-ignored and must never be committed.
# Delete any line this project doesn't actually need.

# Example: an API key for an external model/service used as a baseline or an escalation path.
# OPENAI_API_KEY=
