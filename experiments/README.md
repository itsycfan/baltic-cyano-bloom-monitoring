# experiments/

One folder per research stage, named `stageN_rqX[_rqY]_short-description/`, e.g. `stage2_rq2_information_sufficiency/`. Each stage folder should be runnable on its own (its own scripts and outputs) and its results summarized back into `docs/RQ_MAPPING.md`, not left implicit in code.
