# src/

Core library code, importable by anything under `experiments/`. Put the reusable pieces (data loading, feature extraction, evaluation, visualization) here rather than duplicating them per experiment stage.
